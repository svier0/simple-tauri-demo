@echo off
set "PATH=;%PATH%"
rem pnpm install @deepseek-ai/dsh
rem pnpm approve-builds -all
rem echo @echo off > start.cmd
rem echo set "DSH_HOME=%~dp0.dsh" >> start.cmd
rem echo. >> start.cmd
rem echo ./node_modules/.bin/dsh.CMD web --no-open --port 34333 --trusted-host 127.0.0.1 >> start.cmd
rem echo. >> start.cmd